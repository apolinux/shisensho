# Juego (Tkinter + imágenes)
Pillow>=9.0.0

# Documentación / tooling (opcional)
docutils>=0.16
Pygments>=2.6.1

# HTTP: urllib3 >= 2.7.0 evita reenvío de cabeceras sensibles en redirects
# (Dependabot / GHSA-48p4-8xcf-v9r5 y avisos relacionados)
requests>=2.31.0
urllib3>=2.7.0
